[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "entropyaudit"
version = "1.0.0"
description = "Static auditor for randomness and nonce hygiene in Python source."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "EntropyAudit contributors" }]
